(function () {
  'use strict';

  angular.module('hintsApp.core')
    .factory('observableUtils', observableUtils);

  observableUtils.$inject = ['R','Rx'];
  function observableUtils(R, Rx) {


    return {
    };
  }
}());
