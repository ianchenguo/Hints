(function () {
  'use strict';

  angular.module('hintsApp.core')
    .factory('promiseUtils', promiseUtils);

  promiseUtils.$inject = ['R', 'Promise'];
  function promiseUtils(R, Promise) {

    return {
    };
  }
}());
